columns:
    location_id:
        label: "reuniors.reservations::lang.Location"
        type: text
        select: title
        relation: location
    name:
        label: name
        type: text
        searchable: true
    is_active:
        label: is_active
        type: switch
    activate_at_utc:
        label: activate_at_utc
        type: datetime
    deactivate_at_utc:
        label: deactivate_at_utc
        type: datetime
    discount_value:
        label: discount_value
        type: number
    in_percent:
        label: in_percent
        type: switch
